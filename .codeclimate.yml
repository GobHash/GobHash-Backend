engines:
  eslint:
    enabled: true
    config:
      extensions:
      - .es6
      - .js
    checks:
      import/extensions:
        enabled: false
ratings:
   paths:
   - server/**
   - config/**
   - "**.js"
exclude_paths:
- node_modules/**/*
- server/tests/**
